---
"@cosyte/ccda": patch
---

Split the README's reference sections into files under `documentation/` and link to each from
the heading it left, so the package page stays short enough to read in one sitting. Every
section moved whole and unchanged: no heading was renamed, no text was dropped, and no code,
public API, warning code or emitted document changed.
