---
"@cosyte/ccda": patch
---

Docs — refresh the README status line to current reality (README-ORG-SWEEP). The status banner still
read "pre-alpha (`0.0.x`), not yet published to npm. Through **Phase 5b** the parser ships …"; both
halves were stale. `@cosyte/ccda` is **published on npm at `0.0.1`** and **public**, and the package is
well past Phase 5b (the Phase 7 builder/editor/terminology-adapter surface the same paragraph already
describes). The banner now states: published on npm at `0.0.1`, public, still pre-alpha on the cosyte
`0.0.x` ladder, with the parse → serialize → build → edit → BYO-terminology capability intact.
Documentation only — no code, public-API, or warning-code change.
